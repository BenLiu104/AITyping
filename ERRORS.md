# ERRORS.md — AITyping 錯誤知識庫

> 作用：記錄已踩過的坑，避免新 session / 新 agent 重複犯錯。
> 只放「症狀 → 根因 → 解法 → 預防」。當前狀態見 `STATUS.md`；產品變更歷史見 `CHANGELOG.md`。

| 時間 | 症狀 | 根因 | 解法 | 預防 |
|---|---|---|---|---|
| 2026-07-10 02:48 | `/ws/transcribe-v2` 每個 session 都把 raw PCM 衍生 WAV、事件 JSONL 與 summary 寫入 `/tmp/sv-debug`，不必要地保留語音／逐字稿資料 | `StreamingTranscriptionBridge` 無條件建立 `WsTraceSession`，而 module import 時亦無條件建 trace 目錄 | 預設改用無狀態 no-op trace sink；只有 operator 明確以 `SENSEVOICE_DEBUG_TRACE=1` 啟動 process，或測試注入 trace factory，才建立 disk trace；目錄延遲至 explicit disk trace 才建立 | raw audio／transcript diagnostics 預設必須不落 disk、不保留 raw bytes；診斷只能是 operator-controlled opt-in，且測試要覆蓋 default no-op 與 injected lifecycle |
| 2026-07-08 11:29 | `generate_ephemeral_token(ttl_seconds=-N)` 會計出 past `expire_time`，可能回傳一個已過期的 ephemeral token | TTL resolution 用 `ttl_seconds or settings.LIVE_TOKEN_TTL` + `min(int(ttl), 1800)`，只限制最大值，無限制正值；負數可穿透到 expiry 計算 | adapter 新增 positive integer TTL validation，`None` 才用 settings，`<=0` raise；route query `ttl` 加 `Query(ge=1)`，invalid request 422 before token mint | 任何 expiry/TTL 計算都要同時驗證 lower bound；不要只 `min(max_ttl)` |
| 2026-07-08 11:05 | `/api/live-token` 若簽發 ephemeral token 失敗，有風險把長效 `GEMINI_API_KEY` 當 token 回傳前端 | 舊 adapter 將 SDK token 簽發失敗視為可降級狀態，fallback 到 raw API key 直連 Live API | 移除 raw key fallback；backend 只用 `google-genai` `auth_tokens.create`（`v1alpha`）簽發 Live ephemeral token；失敗時 `/api/live-token` 503 fail closed 且 response 不 echo exception detail | Gemini Live token endpoint must never return `GEMINI_API_KEY` to the frontend. If ephemeral token creation fails, fail closed and investigate SDK/API/version/config：SDK version、`v1alpha` usage、model name、API key permissions、billing/API enablement、Live API availability |
| 2026-06-27 22:00 | Nginx 靜態站對 `/api/` POST 回 405 | frontend Nginx 未配置 API reverse proxy，POST 被當作靜態資源處理 | 在 `nginx.conf` 新增 `location /api/`，proxy 到 `backend:8000` | 部署 frontend 時檢查 API route proxy，不只測 index.html |
| 2026-06-27 22:04 | `pcm-processor.js` 載入 403 | host file permission 是 `600`，nginx container 無法讀取 | `chmod 644 frontend/public/pcm-processor.js` 後 rebuild/redeploy | 新增 public/worklet asset 時確認權限至少 `644` |
| 2026-06-27 22:10 | `/api/live-token` 回 501 / 404（舊紀錄，已被 2026-07-08 security fix 推翻） | 當時誤以為 Google AI Studio API key 無法簽發 Live ephemeral token | 現在改用 `google-genai` `auth_tokens.create` + `v1alpha` 建立 short-lived token，無法簽發時 fail closed | 不要恢復 direct-key fallback；Google AI Studio / Gemini API key 留在 backend，只能用於簽發 ephemeral token |
| 2026-06-27 22:53 | Production UI 退化成裸 HTML | 使用 Tailwind utility class，但 Tailwind 未安裝/未接入 Vite | 安裝 `tailwindcss` / `@tailwindcss/vite`，`index.css` import Tailwind | build 後確認 CSS bundle 不是極小值；必要時用 browser 檢查 computed style |
| 2026-06-27 22:53 | Settings checkbox / options 點不到 | 偽 switch button + label/span pointer interception | 改為原生 checkbox input 覆蓋 switch，decorative spans 加 `pointer-events-none` | 互動元件用 native input + accessibility role，並實測 click |
| 2026-06-27 22:53 | Mic 只走 mock / 不請求真 mic | `mockMode` 預設 true；真流程又先 await token 再 `getUserMedia` | mock 預設 false；真流程先 `getUserMedia()`，成功後才取 token | iOS mic permission 必須在 user gesture 內第一時間請求 |
| 2026-06-27 23:45 | Public URL 間歇 502 | 同一 tunnel 同時跑 host systemd connector + Docker connector；Docker 內 `localhost:8080` 指錯 namespace | 改行 Option A：移除 Docker tunnel service，只保留 host `cloudflared.service` | 不要同一 tunnel token 多 connector 混用；Dashboard ingress 指 `localhost:*` 時用 host connector |
| 2026-06-28 00:06 | Mic 有錄但無 Live transcript / cleanup 空 | Live setup 用 unsupported `TEXT` modality；未啟用 `inputAudioTranscription`；parser 沒讀 `serverContent.inputTranscription.text` | 改 `v1beta` direct WS、`responseModalities: ['AUDIO']`、`inputAudioTranscription: {}`、parse input transcription；release 送 `audioStreamEnd` | Gemini Live input transcription 需要 AUDIO modality + input transcription + 正確 parser |
| 2026-06-28 01:41 | iOS 首次 mic permission 後 recording state 卡住 | permission prompt 打斷 touch sequence；app 在授權前已設 `isRecording=true` | 第一次點 Mic 只做 permission priming，立即 stop tracks，不取 token、不進 recording | iOS 首次 permission prompt 不可當作正式錄音 gesture |
| 2026-06-28 01:55 | PWA 看不到新 Live transcript 行為 / cleanup 假陽性 | 舊 Service Worker 可能仍控制頁面；狀態文字被放入 transcript state | app entry 加 `registerSW({ immediate: true })`；分離 `liveStatus` 與 transcript state；無 true transcript 不 cleanup | PWA update 要 register/claim；status text 不得混入 transcript |
| 2026-06-28 02:13 | iOS mic permission 問題回歸 | localStorage 被當作 permission truth，但 iOS 可能重新 prompt | 移除 localStorage gating；每個 page session 第一 tap 必定只 prime | 不要用 localStorage 判斷 browser permission 真實狀態 |
| 2026-06-28 02:25 | `v02:10` 已更新但 iPhone 仍無 transcript | WebSocket open 不等於 Gemini setup ready；pre-setup buffer 太短，短句開頭易丟 | 增加 `onSetupComplete`；setupComplete 前 buffer 擴到約 5 秒；UI 只在 setupComplete 後提示可說話 | Live audio send/readiness 以 setupComplete 為準，不以 socket open 為準 |
| 2026-06-28 02:48 | Debug 顯示 `ws=1 setup=0 chunks>0 sent=0 tx=0` | Browser WebSocket 可能把 Google Live response 交付成 `Blob`；舊 code 直接 `JSON.parse(event.data)` | LiveClient 先 normalize `string` / `Blob` / `ArrayBuffer` 成 text 再 parse；加 Blob setupComplete regression test | WS client 不可假設 `event.data` 一定是 string |
| 2026-06-28 02:48 | 停止後 debug bytes 仍短暫上升 | AudioWorklet late messages 可能在 disconnect/close 後抵達 | cleanup 時先設 `isCaptureActiveRef=false`、清 port handler；late message 直接 ignore | stop path 要有 synchronous capture gate，不能只靠 async disconnect |
| 2026-06-28 09:19 | Home Screen PWA 已成功 transcript 但仍顯示 WS error | PWA teardown/release 後可能觸發 late `onerror`/1006；原本一律顯示 user-facing error | 若已有 transcript 或 transcript event，WS error 只送 debug telemetry，不 `setErrorMsg`、不中斷 cleanup | 有 transcript 後的 WS error 當 telemetry；不要覆蓋成功流程 |
|| 2026-06-29 03:13 | Live transcript 似乎講完先出、偶爾前半有 transcript 後半缺失 | 前端原本每個 AudioWorklet 128-frame quantum 即送一次 WS JSON/base64，約每 2–3ms 一包；mobile network / Gemini buffer 容易抖動。另 Gemini 3.1 Live 有公開回報 `inputTranscription` 不再像 2.5 一樣 incremental | LiveClient 聚合成約 100ms PCM frames，`audioStreamEnd` 前 flush partial frame；Mixed/Cantonese prompt 限制只輸出繁中/英文，避免日韓文漂移 | transcript pipeline 先看 raw timing/completeness；不要用 cleanup 掩蓋 Live STT 問題；若仍非同步，評估 Gemini 2.5 Live、manual VAD、或替代 streaming STT |
|| 2026-06-30 01:16 | GH Pages deploy 空白頁 / asset 404 | Vite `base` 預設 `/`，但 GitHub Pages project site 用 `/AITyping/` subpath；所有 JS/CSS/manifest path 指向 root 而非 repo subpath | vite.config.ts 加 `base: process.env.GITHUB_ACTIONS ? '/AITyping/' : '/'` | Vite GH Pages 部署必須設定 `base` 為 repo name + trailing slash；市用 `import.meta.env.BASE_URL` 做 runtime asset path |
|| 2026-06-30 01:16 | CORS 更新咗 source code 但 runtime 仍拒絕新 origin | `pydantic-settings` 預設值被 root `.env` 嘅 `ALLOWED_ORIGINS` 環境變數 override；code default 含 `benliu104.github.io` 但 `.env` 只 write 死 `<backend-domain>` | 更新 `.env` 嘅 `ALLOWED_ORIGINS` 加入新 origin，restart container | `pydantic-settings` class default 只是 fallback；`.env` / env var 永遠優先。改 CORS 時一定要檢查 production `.env` 有冇 override |
| 2026-06-30 13:00 | iPhone fetch `Load failed`，後端零請求，幾秒後 error（所有 Content-Type 嘗試都失敗） | iOS NordVPN 在系統網絡層（非瀏覽器層）攔截所有出站連線。`<your-domain>` 被 Threat Protection 判定可疑域名，DNS resolve 被 block，fetch 在手機本地即中斷。與 GitHub Pages origin、CORS、Content-Type 完全無關 | 關閉 NordVPN / 將 domain 加入白名單 | VPN Threat Protection 在 iOS 系統層運作，fetch 目標 domain 係 VPS domain（非 github.io），所以 VPN 一樣會 block。診斷時必須在後端查 log 確認有冇收到任何請求，無請求即代表在手機本地中斷 |
| 2026-06-30 13:00 | Safari `fetch` body 係 Blob 時，`Content-Type: text/plain` 被忽略，實際送出 `audio/wav` | WebKit 已知 bug：fetch body 為 Blob 時，優先採用 Blob 自身 `.type`，覆蓋 headers 設定的 Content-Type。結果觸發 CORS preflight，preflight 對 binary MIME type 在 Safari 內部中斷 | 改用 `ArrayBuffer` 作為 fetch body。`ArrayBuffer` 無內建 MIME type，WebKit 必須採用 header 設定的 `text/plain`，成為真正 simple request，不觸發 preflight | 凡跨域 fetch 二進位數據，body 用 `ArrayBuffer` 不用 `Blob`，避免 WebKit MIME type override |
| 2026-07-01 01:52 | iPhone PWA SenseVoice v2 停頓後漏第二句；stop 後 `tx` 繼續上升到 `close=1000`；trace 顯示 backend 無 `END` / 無 `end_ack` | 前端直接把 iPhone AudioWorklet 2–3ms 細碎 PCM frame 逐包送 WS，streaming VAD / mobile network 行為不穩；stop path 亦可能在未 flush 剩餘 PCM / 未完成 finalize handshake 前斷線 | SenseVoice WS client 改為約 100ms / 3200-byte PCM batching；stop 時先關本地 capture、flush remaining PCM、送 `END`、等待 backend `end_ack`；debug row 顯示 `end` / `ack`；Ben 真機確認 `v01:35` 效果滿意 | 真機 streaming 問題先加 backend trace：chunk size/gap、control `LANG/END`、partial/final/end_ack、reconstructed WAV。不要只靠 UI debug counters 猜；iPhone AudioWorklet 原始 quantum 通常太碎，WS STT client 要 batching |
| 2026-07-06 17:32 | SenseVoice systemd service 搬去新 venv 路徑後 crash loop：`FileNotFoundError: No such file or directory: 'pip'` → `status=6/ABRT`，`/ping` connection refused，NRestarts 一直升 | **根因＝venv 不可搬移**，非「venv 無 pip」（venv 確實有 `pip/pip3/pip3.11`）。`venv/bin/pip` 係 script，shebang 寫死絕對路徑 `#!/home/ubuntu/experiment/voice_test/venv/bin/python3.11`；`mv` venv 後 shebang 仍指舊路徑 → exec pip 搵唔到 interpreter → kernel 報 `ENOENT('pip')`。`bin/python` 只因係 symlink 先冇壞。funasr 載 `FunAudioLLM/SenseVoiceSmall` 時見到 model snapshot 內有 `requirements.txt` 就會每次 startup `pip install -r`（`funasr/utils/install_model_requirements.py`），啱撞正 pip shebang 壞咗 → C++ `terminate` core-dump | 即時 rollback：`mv` venv 返原位（shebang 復位）+ 由 `.bak` 還原 unit + `daemon-reload` + restart，STT 恢復。正確遷移法：**唔好 mv venv**，改為喺目標路徑用 `sensevoice/setup.sh` 重建 in-place venv（shebang 自然指新路徑），再換 unit | venv **永不可搬移／改名**（`bin/` 內所有 console script shebang 寫死絕對路徑）。換路徑＝喺新路徑重跑 `setup.sh` 重建，唔好 `mv`／`cp -r`。改 systemd 前 backup unit + 記 rollback；live service 遷移先喺相同 systemd env（`env -i` + 一樣 PATH）smoke test 開機路徑 |
| 2026-07-08 14:35 | 英文 / 繁中 Gemini Live 連線一直 `1011 (internal error)` 即斷；空 setup `{}` 都一樣 1011 | `v1alpha` **constrained** endpoint（`BidiGenerateContentConstrained`）與非 constrained 不同：它**拒絕 client 送出的任何 setup 內容**（model / generationConfig / systemInstruction 甚至空 `{}`）。真正的 setup 必須在**簽發 ephemeral token 時**鎖入 `auth_tokens.create` 的 `live_connect_constraints.config`，client 只送空 `{setup:{}}` frame 觸發 `setupComplete` | adapter `_build_live_connect_constraints(profile)` 把 `responseModalities` / `inputAudioTranscription` / `systemInstruction`（依 profile）鎖入 token；前端 `sendSetupMessage` 改送 `{setup:{}}`。真 Google endpoint 端到端實測：english / cantonese-english / auto 全部 `setupComplete` | 用 constrained endpoint 時，一切連線參數（含轉錄語言指令）鎖 token，client setup 必須空。改 Live setup 時先分清 constrained vs 非 constrained；斷 1011 而非 close code 4xxx，通常是 setup 內容被 endpoint 整個拒絕，不是個別 field 錯 |
| 2026-07-10 08:12 | `bash check.sh all` exit 0 即使 `cd backend && .venv/bin/ruff format . --check` exit 1（Ruff 格式錯誤被靜默） | `check_phase1()` 的 ruff / pytest 閘門用 `(cmd) && gate ... \|\| skip "not runnable"` 形式：當 cmd return non-zero（工具存在但失敗），`||` 分支同樣是 `skip`，不是 `nogate`。結果 Ruff 格式失敗、pytest 失敗都計 SKIP，overall exit 0 | 改為先偵測 `.venv/bin/ruff` / `.venv/bin/pytest` 是否存在（不存在 → SKIP），存在但 exit non-zero → `nogate`（FAIL）。同時刪除依賴 `source .venv/bin/activate` 的舊寫法，改用絕對路徑直呼叫，避免 subshell activate 不一定成功 | 任何「工具不一定安裝」的閘門，必須分兩層：(1) 工具存在與否（不存在 SKIP）；(2) 工具存在但失敗（FAIL）。`|| skip` pattern 只適用於工具/環境本身缺席，不能 catch 工具執行失敗 |
| 2026-07-08 14:50 | 改咗 backend adapter code、`docker cp` 入 running container、unit test（新開 `docker exec python`）全綠，但打真 route 仍行舊 behaviour（英文 Live route 仍 1011） | running uvicorn 進程**啟動時已 import 舊 module 入 memory**；無 `--reload`，`docker cp` 只改咗 container filesystem，**不會令 running server reload**。`docker exec` 新開 python 會 import 新檔（故 B 測試綠），但 serving 中的 server 仍揸舊 code（故 route A 舊 behaviour）。三方對比 A(route,舊)✗ vs B(exec 新 python,新)✓ vs C(inline,新)✓ 即隔離出「係 running process 未 reload」 | `docker compose up -d --build <svc>` rebuild image + recreate container，令 server 用新 code。**注意**：`docker cp` 會污染 container FS，令「in-container 檔行數 == repo」的 stale-cache proof 失效；改用功能性 proof（rebuild 前 route 1011、rebuild 後 route setupComplete）| 改 running docker 服務的 code，唔好靠 `docker cp`＋`docker exec` 驗證就當上線 —— 個別 exec 進程 import 新 code ≠ serving 進程 reload。要 rebuild + recreate，再打**真 route**（唔係新開 exec）確認新 behaviour。無 `--reload` 的 server 一定要 restart 先生效 |
| 2026-07-13 | `hf repo create liumanben1990/aityping-sensevoice --repo-type space --space-sdk docker` 回 `403 Forbidden`，雖然 `hf auth whoami` 已認到帳號 | shared HF token 可讀取帳號，但冇喺 personal namespace 建立 Space 嘅權限；認證成功唔代表 token 有 repository-create / write scope | 以 Hugging Face account 建立具 write / repository-create 權限嘅 token，登入到 shared `/home/ubuntu/.cache/huggingface/token` 後先重試 create | Space migration preflight 必須同時驗證 `whoami` 同一次最小 `repo create` 權限；token 權限不足時唔好重試同一 create request，先 rotate / replace token |
| 2026-07-13 | 使用 valid `write` token 建立 public CPU Docker Space 時，`hf repo create ... --repo-type space --space-sdk docker --no-private` 回 `402 Payment Required` | HF 實際 API 回覆：free CPU Basic 只支援 Static Space；Docker Space（包括 CPU Basic）需要 PRO subscription，推翻原 migration plan 的免費 CPU Basic 假設 | 停止 create；由 Ben 決定訂閱 HF PRO 或改用其他 host，決定後才重試相應部署路徑 | HF Space migration 在建 repo 前要用實際 create API 驗證 plan-time pricing/entitlement 假設；公開頁面規格或舊紀錄不可取代 account-level API response |